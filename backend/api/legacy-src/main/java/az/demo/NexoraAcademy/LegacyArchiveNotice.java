package az.demo.NexoraAcademy;

/**
 * Historical Java modules retained only as a source-code reference.
 *
 * <p>This source root is intentionally outside {@code src/main/java}; Maven,
 * Spring component scanning, Docker images and runtime endpoints do not include
 * it. The archived public-account, student portal, enrollment, billing, CRM,
 * audit, notification and former Java chatbot code must not be wired back into
 * the active application without a new product decision and security review.</p>
 *
 * @deprecated Reference archive only; not part of the Nexora Academy product.
 */
@Deprecated(forRemoval = true, since = "2026-08")
public final class LegacyArchiveNotice {
    private LegacyArchiveNotice() {
    }
}
